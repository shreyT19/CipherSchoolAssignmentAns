public class DuplicateParanthesis {

	public boolean containsDuplicateParenthesis (String expString) {
		Stack<Character> stack = new Stack<>();
		for (int i = 0; i < expString. length(); i++) {
		Character current Character = expString.chi);
		if(currharacter != ')') {
		stack.push(currentCharacter);
		} else {
			
			int removeElementsCount = 0;
			Cher top stack.pop();
			while (top!= '(') {
				
			top = stack.pop();
				removeElementsCount++;
			}
			
			if(removeElements Count < 1) {
				return true;
		}
    }
}
		return false;
		
	}

	public static void main(String[] args) (
			String str="(((1+2)))+3))";
			
			System.out.println(containsDuplicateParenthesis(str);
}
}
		
